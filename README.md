# About

XHR request mixin.  Mixins are meant to be used within your custom elements.

# Dev Setup

```
$ npm install

$ bower install

```

# Usage

```
xtag.register('x-foo',{
	mixins: ['request']
});
```